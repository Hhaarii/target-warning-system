# Project Instructions & Rules

## Automatic GitHub Upload Rule
Whenever the user specifies "upload to git", "commit to git", or "add to git":
1. Always stage and commit files locally (`git add . && git commit`).
2. Always push directly to online GitHub (`git push origin master` or `gh repo create Hhaarii/... --public --push`).
